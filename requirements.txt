# Core dependencies
fastmcp>=0.1.0

# CLI dependencies (optional but recommended)
typer>=0.9.0
rich>=13.0.0

# Configuration dependencies (optional but recommended)
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Schema validation (optional)
pydantic>=2.0.0
